<?php
   $subst_model = new Substances();

   $ids = $subst_model->test();

   foreach($ids as $r)
   {
       $s = new Substances($r->id);

       $struc = file_get_contents(File::FOLDER_3DSTRUCTURES . $s->identifier . ".mol");

     $final_folder = "media/inoke/intersection/";

     if(!is_dir($final_folder))
       {
           mkdir($final_folder, 0777, True);
       }

     file_put_contents($final_folder . $s->name . '.mol', $struc);
   }
    die();

?>

<?php if(isset($_SESSION['user']) && $_SESSION['user']['admin'] == 1) : ?>
    <h4><a href="/edit/article"><div style="text-align: right;"> <span class="glyphicon glyphicon-edit"></span> Edit</div></a></h4>
<?php endif?>

<div><?=$nonsec_content?></div>

<script src="js/validator.js?ver=<?=JS_VERSION?>"></script>